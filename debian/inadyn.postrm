#!/bin/sh

set -e

NAME=inadyn
LOGDIR=/var/log/$NAME
CONF=/etc/$NAME.conf

if [ "$1" = "purge" ]
then
	rm -rf $RUNDIR $LOGDIR
	for i in $LOGDIR $CONF
	do
		dpkg-statoverride --remove $i 2>/dev/null || true
	done
fi

#DEBHELPER#

exit 0

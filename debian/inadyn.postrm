#!/bin/sh

set -e

NAME=inadyn
RUNDIR=/var/run/$NAME
CONF=/etc/$NAME.conf

if [ "$1" = "purge" ]
then
	if [ -d $RUNDIR ]; then
	    rm -rf $RUNDIR
	fi
	dpkg-statoverride --remove $i 2>/dev/null || true
fi

#DEBHELPER#

exit 0
